services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - SUPABASE_URL=https://cyrgzvnjvevwbjqxgfxd.supabase.co
      - SUPABASE_ANON_KEY=sb_publishable_-UHOOC1BcmetUpRiKLPJrA__yjHojBN
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY:-}
      - GOOGLE_PLACES_API_KEY=${GOOGLE_PLACES_API_KEY:-}
      - FLIGHTAWARE_API_KEY=${FLIGHTAWARE_API_KEY:-}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s